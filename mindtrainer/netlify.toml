# Netlify build settings. In the Netlify UI, set the site's
# "Base directory" to  mindtrainer  so these paths resolve.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
